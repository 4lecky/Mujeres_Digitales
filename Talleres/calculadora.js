const rl = require('readline').createInterface({
    input: process.stdin,
    output: process.stdout,
});

console.log('Calculadora');

const numUno = await preguntar('El primer número:');
const numDos = await preguntar('El segundo número:');

const memuu = await preguntar('Elija la operación a realizar: \n 1) Sumar \n 2) Restar \n 3) Multiplicar \n 4) Dividir \n');



switch (key) {
    case value:
        
        break;

    default:
        break;
}